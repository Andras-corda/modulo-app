# Clé admin pour les routes protégées (POST/PUT/DELETE /api/modules, etc.)
ADMIN_KEY=change-me-secret-key

# URL publique Railway (utilisée pour construire les scriptUrl)
RAILWAY_URL=https://modulo-web-production.up.railway.app

# Port (Railway l'injecte automatiquement)
PORT=3000
